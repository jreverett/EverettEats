# EverettEats
Personal site for hosting recipes

## Local Development

Requires [.NET 10 SDK](https://dotnet.microsoft.com/download/dotnet/10.0).

```bash
dotnet run
```

The site will be available at `http://localhost:5259`.

For hot reload during development:

```bash
dotnet watch
```
